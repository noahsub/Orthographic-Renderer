<UserControl
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d"
    x:Class="Orthographic.Renderer.Pages.LightingPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:controls="clr-namespace:Orthographic.Renderer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="75" />
        </Grid.RowDefinitions>

        <Rectangle
            Fill="{StaticResource SecondaryBackground}"
            Grid.Row="0"
            Grid.RowSpan="2"
            Opacity="{StaticResource GlobalOpacity}" />
        <Rectangle Fill="{StaticResource PrimaryBackground}" Grid.Row="1" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <controls:LightingGridSelector
                    Grid.Column="0"
                    Grid.Row="0"
                    Padding="20,20,20,0"
                    x:Name="TopLightingGridSelector" />
                <controls:LightingGridSelector
                    Grid.Column="1"
                    Grid.Row="0"
                    Padding="20,20,20,0"
                    x:Name="BottomLightingGridSelector" />
                <controls:LightingGridSelector
                    Grid.Column="2"
                    Grid.Row="0"
                    Padding="20,20,20,0"
                    x:Name="FrontLightingGridSelector" />

                <controls:LightingGridSelector
                    Grid.Column="0"
                    Grid.Row="1"
                    Padding="20,0,20,20"
                    x:Name="BackLightingGridSelector" />
                <controls:LightingGridSelector
                    Grid.Column="1"
                    Grid.Row="1"
                    Padding="20,0,20,20"
                    x:Name="LeftLightingGridSelector" />
                <controls:LightingGridSelector
                    Grid.Column="2"
                    Grid.Row="1"
                    Padding="20,0,20,20"
                    x:Name="RightLightingGridSelector" />
            </Grid>

        </Grid>



        <!--  BOTTOM GRID  -->
        <Grid
            ColumnDefinitions="20,100,*,100,20"
            Grid.Row="1"
            VerticalAlignment="Center">
            <Button
                Click="BackButton_OnClick"
                Content="Model"
                Grid.Column="1"
                HorizontalContentAlignment="Center"
                VerticalAlignment="Center"
                Width="100"
                x:Name="BackButton" />
            <Label
                Content="File"
                Grid.Column="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                x:Name="FileLabel" />
            <Button
                Click="ViewsButton_OnClick"
                Content="Views"
                Grid.Column="3"
                HorizontalContentAlignment="Center"
                VerticalAlignment="Center"
                Width="100"
                x:Name="ViewsButton" />
        </Grid>
    </Grid>
</UserControl>
